import React from 'react'

export default function TrackPageDetail({ params }: { params: { slug: string } }) {
    console.log('check====>',params.slug)
  return (
    <div>TrackPageDetail</div>
  )
}
